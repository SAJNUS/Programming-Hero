/**
 * 2 data types
 * 1. primitive (string, number, boolean, undefined, null)
 * 2. non-primitive or referential (array, object)
 */


// primitive data type
let name = "sajnus"; //string
let id = 5810; //number (integer)
let weight = 70.5; //number (floating)
let isMarried = true; //boolean
let area;
let address = null;

console.log(name, id, weight, isMarried, area, address);
